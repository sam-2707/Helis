# Supabase (optional for demo mode — app runs with in-memory seed data without these)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI (required for digest generation; falls back to template text if missing)
OPENAI_API_KEY=

# WhatsApp Business API (optional — mock sender logs to console without these)
WHATSAPP_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_MOCK=true
